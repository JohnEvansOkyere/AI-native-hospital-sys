fastapi==0.115.0
uvicorn[standard]==0.30.6
aiosqlite==0.20.0
groq==0.13.0
python-dotenv==1.0.1
pydantic==2.9.2
requests>=2.31
python-multipart>=0.0.9
httpx>=0.27

# Turso needs no client library: db.py speaks Hrana over HTTP (/v2/pipeline)
# using httpx above. libsql-client is deliberately not used — its HTTP transport
# still posts to the retired v1/execute endpoint and fails against current Turso.

# Speech-to-text providers. All optional and loaded lazily — the app degrades to
# "I couldn't hear that, please type it" when none are available.
#   openai  -> hosted whisper-1 (needs OPENAI_API_KEY)
# Intron Sahara and Cartesia need no package at all, just their API keys.
openai>=1.30

# NOTE: faster-whisper (local, offline, no key) is deliberately NOT here.
# It pulls ~200MB+ of native deps and downloads model weights to disk at first
# use, which exceeds the serverless function size limit and has nowhere to write.
# It stays in the stt.py provider chain and is installed for local runs and the
# benchmark by requirements-local.txt — offline capability is unchanged off-Vercel.
